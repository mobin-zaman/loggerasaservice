import React from "react";

const ApplicationDashBoard = ({name, apiKey}) => {
    return (
        <div>
            name of the application {name}
            and the api key {apiKey}
        </div>
    )

}

export default  ApplicationDashBoard;
